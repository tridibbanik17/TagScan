# This is a basic example. You can replace it with your actual data processing logic.
def process_data(data):
    # Here, we're just returning the data with a simple prefix for illustration
    return f"Processed data: {data}"
